export interface Group {
  id: string;
  title: string;
}

export interface Filter {
  id: string;
  title: string;
}

export interface Player {
  id: string;
  name: string;
  time: Time;
}

export interface Time {
  id: string;
  title: string;
}
